# port-go-sse
Server-Sent Event (SSE) with Go Language

Check `compose.yaml` file as example for testing this SSE.

Access this SSE using `:6969/sse?delay=1000` endpoint.

Query Parameter:
- delay is millisecond and the minimum value of delay is 1
